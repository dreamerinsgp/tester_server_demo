apiVersion: v1
kind: Service
metadata:
  name: demo-server
  namespace: staging
spec:
  type: NodePort
  selector:
    app: demo-server
  ports:
    - port: 5000
      targetPort: 5000
      nodePort: 30080
